# Frontend

React application (created with Create React App).

## Quick start (development)
```bash
cd frontend
npm install
# Start dev server
npm start
```

## Build for production
```bash
cd frontend
npm run build
```

The production build output is placed in `frontend/build` which the backend
will serve if present.